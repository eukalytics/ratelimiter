import React from 'react';

export default function SubmitBtn() {
    return(
        <div>
            <button id="submitBtn" className="bg-green-300 hover:bg-green-400 py-4 px-8 rounded-full">Submit</button>
        </div>
    );
}